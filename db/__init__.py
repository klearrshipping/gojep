"""
Database package.

Submodules:
  db.client   — Supabase REST client (SupabaseClient)
  db.emails   — Email pipeline helpers (email_updates, email_actions)
  db.tenders  — Tender row mapping
  db.awards   — Award row mapping
  db.migrations  — SQL migration scripts
  db.schemas     — SQL schema definitions
"""
